# Codex Taskboard

本地优先的议题工作台。任务处理耗时描述议题实际处于处理中的累计时间，与个人投入工时、Jira 工作日志和 Agent 运行时间相互独立。

## Language

**处理中**:
议题已经开始被处理、但尚未进入等待确认、阻塞或结束状态的工作流阶段。
_Avoid_: Coding、开发中、In Progress（这些是外部系统可能使用的状态名）

**任务处理耗时**:
议题处于处理中且未被本地暂停的累计时间，允许多张议题并行累计。
_Avoid_: 个人工时、工作台在线、已处理、任务占用时长

**计时片段**:
一段从开始或继续到暂停或离开处理中的连续任务处理时间。
_Avoid_: 工时切片、会话、run、session

**本地暂停**:
议题仍处于处理中、外部工作流状态不变，但任务处理耗时停止累计的本地状态。
_Avoid_: 暂停处理、blocked、Jira 暂停

**等待用户输入**:
任务会话仍然可以继续执行，但当前 turn 需要用户在任务会话窗口回答问题或做出决定的生命周期状态。
_Avoid_: 阻塞、失败、暂停

**任务日耗时**:
一张议题在某一自然日内所有计时片段的总和。
_Avoid_: 当日个人工时

**项目累计处理耗时**:
一个项目内所有议题在选定日期的任务日耗时之和；并行处理时可以超过同一日期的自然时间。
_Avoid_: 项目实际工作时长、项目在线时长

**当日结束处理任务数**:
在某一自然日内因离开处理中而停止计时的去重议题数；本地暂停不计入。
_Avoid_: 完成任务数、关闭任务数、当日产出

**Agent 运行时长**:
Agent 会话持续运行的时间，与任务处理耗时相互独立。
_Avoid_: 已处理、任务处理耗时

**Jira 本地状态覆盖**:
Jira 当前工作流没有可达目标状态时，由 Taskboard 显式保留的本地任务状态；只要远端 Jira 状态未变化，常规同步就不会覆盖它。
_Avoid_: Jira 状态、同步失败、离线状态

**主会话**:
围绕且只围绕一张议题负责读取 Jira 上下文、提炼任务意图、管理执行会话通信、检查结果并发起 Jira 写回的 Codex 会话。
_Avoid_: 执行会话、子任务会话、普通任务对话

**任务会话**:
由主会话派生并在独立 Codex 窗口中运行、只负责完成已确认任务意图的 Codex 会话；除非主会话明确补充要求，否则不重新分析 Jira。
_Avoid_: 主会话、临时对话、普通新对话

**任务会话窗口**:
承载任务会话的独立 Codex 窗口，允许用户与执行 Agent 多轮交流、执行和修正；窗口关闭后仍可通过任务编排记录恢复同一个任务会话。
_Avoid_: 主窗口、弹窗、临时窗口

**任务意图**:
由主会话从 Jira 描述、评论、附件和关联信息中提炼出的可执行目标，包含范围、验收标准、约束和待确认问题。
_Avoid_: Jira 原文、任务摘要、提示词

**意图过期**:
任务意图对应的 Jira 快照或关键摘要已发生变化，原意图不能继续用于派发或结果验收，必须重新读取并确认。
_Avoid_: 需求变更已自动合并、旧摘要继续执行

**附件引用**:
主会话为任务会话授权的、指向必要 Jira 附件的受限读取入口；它不是附件副本，也不包含 Jira 凭据。
_Avoid_: 全量附件复制、Jira token、完整本地路径

**执行配置**:
任务会话派发时确定的模型、推理级别、技能、权限和执行环境；默认继承主会话，派发前可覆盖，运行中变更必须形成新版本。
_Avoid_: 静默提权、运行中换模型、未记录的执行环境

**结果检查**:
主会话在收到结果包后执行的证据核对和质量判断；可以自动启动，但不会自动通过、集成代码或写回 Jira。
_Avoid_: 报告已收到、自动验收、自动写回

**返工**:
主会话基于检查结论向同一任务会话提出的补充执行要求；沿用原意图编排、任务会话和 worktree，并以新的结果版本再次报告。
_Avoid_: 新建重复会话、覆盖旧结果、修改原结果

**执行尝试**:
同一会话编排中的一次具体任务会话运行记录；恢复优先沿用原尝试，只有原 thread 或 worktree 无法恢复时才创建新尝试，并保留旧尝试证据。
_Avoid_: 覆盖旧尝试、无关联重试、新建编排

**结果包**:
任务会话回传给主会话的结构化执行结果。用户可见报告只突出做了什么、为什么这样做，并仅在存在风险时显示风险；协议仍保留变更文件、验证证据、阻塞状态和版本信息，供主会话检查与审计。
_Avoid_: 最终回复、聊天记录、Jira 评论

**会话编排**:
围绕一张议题维护主会话与任务会话的派生关系、意图投递、进度通知、结果回传和 Jira 写回状态。
_Avoid_: 会话列表、对话绑定、自动认领

**执行隔离**:
每个任务会话使用独立的工作目录和分支；不同议题可以并行执行，但是否存在文件、功能或运行时冲突由用户判断。
_Avoid_: 自动排队、共享工作区、并行子会话

**代码集成**:
任务会话分支通过主会话检查后，由用户确认合并或挑选提交进入主工作区的动作；结果通过不等于代码已集成。
_Avoid_: 自动合并、结果通过、直接写入主工作区

**完成确认**:
在结果检查、代码集成和 Jira 写回完成后，由用户显式确认整个任务闭环完成，Taskboard 才能将议题标记为 `done`。
_Avoid_: 自动完成、Jira 写回成功即完成、代码集成即完成
