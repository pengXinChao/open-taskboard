---
status: accepted
---

# Taskboard 主会话与任务会话的编排边界

Taskboard 第一阶段采用“一主一子、独立窗口、独立 worktree”的任务会话编排：主会话只负责 Jira 上下文读取、任务意图确认、结果检查和用户确认后的 Jira 写回；任务会话负责执行并在用户确认报告后回传结果。结果检查、代码集成、Jira 写回和任务完成彼此分离，均不因子会话结束而自动发生。该边界在独立窗口体验、主会话管理权和执行隔离之间取得平衡，并保留后续扩展多 worker、系统通知和 PR 集成的空间。
